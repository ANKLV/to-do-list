<div class="container">
  <p>
    <span class="title"><%= task.title %></span>
    <%= link_to 'Edit', edit_task_path(task), class: 'btn btn-secondary btn-sm' %>
    <%= link_to 'Delete', task_path(task), method: :delete, class: 'btn btn-secondary btn-sm' %>
  </p>
</div>
